language: java

branches:
    only:
        - master
script:
- mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
deploy:
  # Upload binaries
  - provider: s3
    on:
      branch:
        - master
    skip_cleanup: false
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_ACCESS_SECRET
    bucket: "downloads.ortussolutions.com"
    local-dir: target
    upload-dir: cfmlprojects/urlrewritefilter
    acl: public_read